---
title: "VimでCSVを気持ちよく編集したくて「Tirenvi」を作った話"
emoji: "🦁"
type: "tech"
topics:
  - "vim"
  - "neovim"
  - "csv"
  - "プラグイン"
published: true
published_at: "2026-03-02 07:56"
---

皆さんは表形式のデータをどう管理していますか？

スプレッドシートは高機能で便利ですが、
- ファイル形式が独自
- Gitでdiffが取りづらい
- マウス操作が前提

といった点が、私には少し扱いづらく感じられました。

やはりテキストデータとして管理して、Gitで差分を確認できる状態にしておきたい。
できればキーボードだけで完結させたい。

となると、CSV や TSV が自然な選択肢になります。
理想は「Vimで開いて、そのまま快適に編集できること」です。

---

## 既存ツールの検討

Vim用のCSVプラグインはいくつか試してみましたが、
- 表示が崩れやすい
- 編集体験がいまひとつ
- 操作が独自すぎる

など、どうもしっくり来ませんでした。

Vimにこだわらなければ、VisiData という素晴らしいツールがあります。
実際に使ってみましたが、独自の操作体系を覚える必要があり、
「いつものVimの操作感」とは少し違います。

---

## それなら自分で作ろう

不満があるなら、自分で作るのが早い。

そうしてできたのが、Neovimプラグイン **Tirenvi** です。

https://github.com/kibi2/tirenvi.nvim

---

## Tirenviの特徴

- CSV/TSVを構造を保ったまま整形表示
- Vimの通常コマンドがそのまま全部使える
- 編集で列構造が壊れても自動補正
- バッファは純粋なテキストのみ
- Undo treeを壊さない

## デモ

![](https://storage.googleapis.com/zenn-user-upload/3bfcb135c7da-20260302.gif)


行編集、列編集、セル編集、undo/redoすべて「いつものVim」です。

特別な操作を覚える必要はありません。

## 仕組み

内部では TIR (Tabular Intermediate Representation) という中間表現を使っています。

```text
flat (CSV, TSV, ...)
        ↓ 外部パーサー(tir-csvコマンドなど)
TIR (intermediate representation)
        ↓ Tirenvi (Neovimプラグイン)
tir-vim (structured buffer view)
```

保存する時は逆に動きます。

```
tir-vim → TIR → flat (保存先のフォーマット)
```

Tirenviの設計思想はシンプルです。

**編集するのはテキスト**
**でも構造は常に保証する**

tir-vim と TIR は完全可逆変換です。
編集するのはテキスト (tir-vim) です。
しかし構造 (TIR) は常に保証されます。

その結果、diffは常に安定します。

TIRとflatの変換は外部パーサーが担当します。
パーサー側を賢くすれば、情報落ちも防げます。

Excel, HTML, Org-mode, RDB, txt, ... 
パーサーさえあれば、すべて同じ編集体験になります。

TirenviはCSVを整形表示するプラグインではありません。
構造を壊さずテキスト編集するためのレイヤーです。

## インストール

### lazy.nvim

```lua
{
  "kibi2/tirenvi.nvim",
  config = function()
    require("tirenvi").setup()
  end,
}
```

### CSVパーサーのインストール

```bash
pip install tir-csv
```

## 今後

次は Markdown (GFM) のテーブルもVimでサクサク編集できるようにしたいです。
先月作った「ブラウザの入力をVim化するツール」Wenviと併用すればGitHubで表を書くのも快適になるでしょう。

https://zenn.dev/ogwk/articles/f5b1d69640af68

興味があれば、ぜひ試してみてください。
